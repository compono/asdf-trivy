name: Release

on:
  push:
    branches:
      - master

jobs:
  release-please:
    runs-on: ubuntu-slim
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: simple
